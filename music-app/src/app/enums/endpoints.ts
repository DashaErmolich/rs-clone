export enum Endpoints {
  track = '/track',
  artist = '/artist',
  album = '/album',
  editorial = '/editorial',
  search = '/search',
  chart = '/chart',
  genre = '/genre',
  radio = '/radio',
  playlist = '/playlist',
  artists = '/artists',
}

export enum SearchType {
  tracks = 'tracks',
  artists = 'artists',
  albums = 'albums',
  playlists = 'playlists',
}
